import { BatchRenderer, ViewableBuffer, Buffer, Geometry, ExtensionType, Renderer } from '@pixi/core';
export declare class Batch3dGeometry extends Geometry {
    _buffer: Buffer;
    _indexBuffer: Buffer;
    constructor(_static?: boolean);
}
export declare class Batch2dRenderer extends BatchRenderer {
    static extension: {
        name: string;
        type: ExtensionType;
    };
    constructor(renderer: Renderer);
    setShaderGenerator(): void;
    packInterleavedGeometry(element: any, attributeBuffer: ViewableBuffer, indexBuffer: Uint16Array, aIndex: number, iIndex: number): void;
}
