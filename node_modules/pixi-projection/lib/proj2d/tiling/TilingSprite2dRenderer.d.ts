import { ExtensionMetadata, ObjectRenderer, QuadUv, Renderer, Shader } from '@pixi/core';
export declare class TilingSprite2dRenderer extends ObjectRenderer {
    static extension: ExtensionMetadata;
    constructor(renderer: Renderer);
    shader: Shader;
    simpleShader: Shader;
    quad: QuadUv;
    render(ts: any): void;
}
