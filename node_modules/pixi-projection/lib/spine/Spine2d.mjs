import { Graphics } from '@pixi/graphics';
import '../proj2d/index.mjs';
import { SimpleMesh2d } from '../proj2d/mesh/Mesh2d.mjs';
import { Container2d } from '../proj2d/Container2d.mjs';
import { Sprite2d } from '../proj2d/sprites/Sprite2d.mjs';

function applySpine2dMixin(spineClassPrototype) {
  spineClassPrototype.newMesh = function newMesh(texture, vertices, uvs, indices, drawMode) {
    return new SimpleMesh2d(texture, vertices, uvs, indices, drawMode);
  };
  spineClassPrototype.newContainer = function newMesh() {
    if (!this.proj) {
      this.convertTo2d();
    }
    return new Container2d();
  };
  spineClassPrototype.newSprite = function newSprite(texture) {
    return new Sprite2d(texture);
  };
  spineClassPrototype.newGraphics = function newMesh() {
    const graphics = new Graphics();
    graphics.convertTo2d();
    return graphics;
  };
  spineClassPrototype.transformHack = function transformHack() {
    return 2;
  };
}

export { applySpine2dMixin };
//# sourceMappingURL=Spine2d.mjs.map
