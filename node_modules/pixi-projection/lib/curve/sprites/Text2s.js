'use strict';

var text = require('@pixi/text');
var ProjectionSurface = require('../ProjectionSurface.js');
var math = require('@pixi/math');
var Sprite2s = require('./Sprite2s.js');

class Text2s extends text.Text {
  constructor(text, style, canvas) {
    super(text, style, canvas);
    this.aTrans = new math.Matrix();
    this.proj = new ProjectionSurface.ProjectionSurface(this.transform);
    this.pluginName = "batch_bilinear";
  }
  get worldTransform() {
    return this.proj;
  }
}
Text2s.prototype.calculateVertices = Sprite2s.Sprite2s.prototype.calculateVertices;
Text2s.prototype.calculateTrimmedVertices = Sprite2s.Sprite2s.prototype.calculateTrimmedVertices;
Text2s.prototype._calculateBounds = Sprite2s.Sprite2s.prototype._calculateBounds;

exports.Text2s = Text2s;
//# sourceMappingURL=Text2s.js.map
